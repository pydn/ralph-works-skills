{
  "name": "danger-gate-extension",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "test": "node --import jiti/register --test tests/danger-gate/*.test.ts"
  },
  "devDependencies": {
    "jiti": "^2.7.0"
  }
}
